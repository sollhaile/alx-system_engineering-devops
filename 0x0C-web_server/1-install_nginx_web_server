#!/usr/bin/env bash
# Install and configure nginx to serve "Hello World!" at /

# Exit on error
set -e

# Update package list
apt-get update -y

# Install nginx
apt-get install -y nginx

# Replace the default web root page
echo 'Hello World!' > /var/www/html/index.html

# Start nginx using service (not systemctl)
service nginx start

















